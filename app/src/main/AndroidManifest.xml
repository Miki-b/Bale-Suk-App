<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BaleSuk"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".ui.activities.OrderActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.Register"
            android:exported="false" />
        <activity
            android:name=".ui.activities.Home"
            android:exported="false" />
        <activity
            android:name=".ui.activities.WorkoutList"
            android:exported="false" />
        <activity
            android:name=".ui.activities.AddProduct"
            android:exported="false" />
        <activity
            android:name=".ui.activities.ProductDetail"
            android:exported="false" />
        <activity
            android:name=".ui.activities.CatagoriesProductList"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity android:name=".ui.activities.CartActivity" />
        <activity
            android:name=".ui.activities.Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>